# PostgreSQL Database Configuration
DB_HOST=localhost
DB_USER=pixelart_user
DB_PASSWORD=pixelart_password
DB_NAME=pixelart_db
DB_PORT=5432

# Server Configuration
PORT=3001

# Note: Copy this file to .env and update with your PostgreSQL credentials
# Make sure PostgreSQL server is running and the user has proper permissions
