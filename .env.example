# Firebase Configuration (Optional - for cloud sync)
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Supabase Configuration (Recommended - for automatic cross-device sync)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server Configuration
# For local development (same computer)
VITE_CUSTOM_API_URL=http://localhost:3001/api

# For network access (other computers on same network)
# Replace 192.168.1.100 with your computer's IP address
# VITE_CUSTOM_API_URL=http://192.168.1.100:3001/api

VITE_CUSTOM_API_KEY=your_custom_api_key

# GitHub Configuration (Optional - for automatic cross-device sync)
VITE_GITHUB_TOKEN=your_github_personal_access_token
VITE_GITHUB_USERNAME=your_github_username

# Note: The system works without these environment variables
# It will fall back to local storage and manual backup/restore
#
# To enable automatic cross-device sync:
# 1. Create a GitHub Personal Access Token with 'gist' scope
# 2. Add your token and username to the .env file
# 3. Data will automatically sync between devices via private GitHub Gists
